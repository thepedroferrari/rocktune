/**
 * UI timing constants for animations and debouncing
 */
export const UI_DELAYS = {
  CARD_ANIMATION_MS: 30,
  SEARCH_ANNOUNCE_MS: 500,
  FILTER_ANIMATION_MS: 20,
} as const

/**
 * Filter value constants
 */
export const FILTER_VALUES = {
  WILDCARD: '*',
  SELECTED: 'selected',
} as const
